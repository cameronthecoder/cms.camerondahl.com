Django>=5.2,<5.3
wagtail>=7.4,<7.5  # 7.4 is an LTS release

# Explicitly listed in INSTALLED_APPS
django-cors-headers>=4.9,<5.0
djangorestframework>=3.16,<4.0
django-filter>=25.2

# Database
dj-database-url>=2.1,<3.0
psycopg[binary]>=3.1,<4.0

# Loads variables from a local .env file (no-op if the file doesn't exist,
# so it's safe in production where real env vars are set by the platform)
python-dotenv>=1.0,<2.0

# Magic-link ("passwordless") login for the admin
django-sesame>=3.2,<4.0
